# Railway Deployment

## Settings

**Root Directory:** `backend`

**Build Command:** (auto-detected from nixpacks.toml)

**Start Command:** (auto-detected from Procfile)

## Environment Variables

Add all variables from your `.env` file in Railway Dashboard → Variables

## Deploy

Push to main branch - Railway will auto-deploy.
